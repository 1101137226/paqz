using UnityEngine;
using System.Collections;

public class hittwice : MonoBehaviour {
	public byte hitcount;
	// Use this for initialization
	void Start () {
		hitcount = 0;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
